<?php
    $conexao = pg_connect("dbname=jmbnetdb port=5433 user=ventura password=172349av");
    $query = "select nome_modulo from modulo";
    $result = pg_query($conexao,$query);
    for ($i=0,$i<pg_num_rows($result),$i++){
            
    }
    
    
?>